import { z } from 'zod';
import { BriefData, TiptapNode } from '@scopeprofit/contracts';
import { fail } from '../../security';
const text=z.string().max(10000);const list=z.array(text).max(200);
export const briefSchema=z.object({summary:text,requirements:z.array(z.object({id:text,description:text,type:z.enum(['functional','rule','data','integration','nonfunctional']),priority:z.enum(['must','should']),source:text,sourceMessageId:z.string().uuid(),systemNote:text})).max(200),questions:z.array(z.object({id:text,question:text,reason:text,blocksEstimate:z.boolean()})).max(100),risks:z.array(z.object({id:text,description:text,impact:text,mitigation:text,severity:z.enum(['red','yellow','green'])})).max(100),included:list,excluded:list,assumptions:list,acceptanceCriteria:list,estimates:z.array(z.object({module:text,minHours:z.number().finite().min(0).max(100000),maxHours:z.number().finite().min(0).max(100000),uncertainty:text})).max(100),nextSteps:list}).strict();
export function validateBrief(data:unknown):BriefData {const result=briefSchema.safeParse(data);if(!result.success)fail('INVALID_BRIEF');for(const estimate of result.data.estimates)if(estimate.maxHours<estimate.minHours)fail('INVALID_ESTIMATE');return result.data;}
export function validateSources(brief:BriefData,messages:{id:string;content:string}[]) {for(const requirement of brief.requirements){const source=messages.find(m=>m.id===requirement.sourceMessageId);if(!source||!requirement.source.trim()||!source.content.includes(requirement.source))fail('INVALID_REQUIREMENT_SOURCE');}}
export function validateEditor(node:unknown):TiptapNode {let count=0;const allowed=new Set(['doc','paragraph','heading','bulletList','orderedList','listItem','table','tableRow','tableHeader','tableCell','text','hardBreak']);function walk(value:any,depth:number){if(++count>10000||depth>20||!value||typeof value!=='object'||!allowed.has(value.type))fail('INVALID_EDITOR_CONTENT');if(value.text!==undefined&&(value.type!=='text'||typeof value.text!=='string'||value.text.length>10000))fail('INVALID_EDITOR_CONTENT');if(value.attrs){const attrs=Object.keys(value.attrs);if(attrs.some(k=>!['level','colspan','rowspan','colwidth'].includes(k)))fail('INVALID_EDITOR_CONTENT');if(value.attrs.level&&![1,2,3].includes(value.attrs.level))fail('INVALID_EDITOR_CONTENT');}if(value.marks&&(!Array.isArray(value.marks)||value.marks.some((m:any)=>!['bold','italic','strike','code','underline'].includes(m.type)||m.attrs)))fail('INVALID_EDITOR_CONTENT');if(value.content){if(!Array.isArray(value.content))fail('INVALID_EDITOR_CONTENT');value.content.forEach((child:any)=>walk(child,depth+1));}}walk(node,0);if((node as TiptapNode).type!=='doc'||JSON.stringify(node).length>1000000)fail('INVALID_EDITOR_CONTENT');return node as TiptapNode;}
