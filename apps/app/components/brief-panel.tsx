'use client';
import { useState } from 'react';
import type { Brief, BriefData, DocumentVersion, TiptapNode } from '@scopeprofit/contracts';
import { api, errorMessage } from '@/lib/api';
import { documentText, textDocument } from '@/lib/project';
import { RichEditor } from './rich-editor';
const editableSections=[['summary','Resumen ejecutivo'],['included','Incluidos'],['excluded','Excluidos'],['assumptions','Supuestos'],['acceptanceCriteria','Criterios de aceptación'],['nextSteps','Próximos pasos']] as const;
export function BriefPanel({projectId,brief,document,professional,token,locked,onSaved}:{projectId:string;brief?:Brief;document?:DocumentVersion|null;professional:boolean;token?:string;locked:boolean;onSaved:()=>Promise<void>}){
 const [editing,setEditing]=useState(false);const[draft,setDraft]=useState<BriefData|null>(null);const[baseVersion,setBaseVersion]=useState(0);const[editorContent,setEditorContent]=useState<TiptapNode|null>(null);const[busy,setBusy]=useState(false);const[error,setError]=useState('');
 function edit(){if(!brief)return;setDraft(structuredClone(brief.data));setBaseVersion(brief.version);setEditorContent(professional?document?.editorContent??null:null);setEditing(true);setError('');}
 async function save(){if(!draft)return;setBusy(true);setError('');try{if(professional&&editorContent&&document){await api(`/projects/${projectId}/documents/current`,{method:'PATCH',body:JSON.stringify({expectedVersion:document.version,editorContent})},token);}else{await api(`/projects/${projectId}/brief`,{method:'PATCH',body:JSON.stringify({expectedVersion:baseVersion,data:draft})},token);}setEditing(false);await onSaved();}catch(e){setError(errorMessage(e));}finally{setBusy(false);}}
 const data=brief?.data;
 return <section className="brief-panel"><div className="panel-heading"><div><p className="eyebrow">DOCUMENTO VIVO</p><h2>Brief del proyecto</h2></div><span className="tag">v{brief?.version??0}</span></div><p className="small muted">Borrador de trabajo. La aprobación final corresponde al profesional.</p>{error&&<p role="alert" className="error">{error}</p>}{editing&&draft?<><div className="notice">{professional?'Podés editar todo el documento.':'Editá el resumen, el alcance y los próximos pasos. Para cambiar requisitos o estimaciones, escribí por el chat.'} Tu edición se conserva si hay un conflicto.</div>{professional&&editorContent?<RichEditor content={editorContent} onChange={setEditorContent} full label="Documento completo"/>:editableSections.map(([key,label])=><div className="edit-section" key={key}><h3>{label}</h3><RichEditor label={label} content={textDocument(key==='summary'?draft.summary:(draft[key] as string[]).join('\n'))} onChange={node=>setDraft(prev=>prev?{...prev,[key]:key==='summary'?documentText(node):documentText(node).split('\n').filter(line=>line.trim())}:prev)}/></div>)}<div className="actions sticky-actions"><button className="button" disabled={busy} onClick={save}>{busy?'Guardando…':'Guardar nueva versión'}</button><button className="button secondary" disabled={busy} onClick={()=>setEditing(false)}>Cancelar</button></div></>:data?<div className="brief-sections"><section><h3><span>01</span> Resumen ejecutivo</h3><p className="preserve">{data.summary||'El resumen se construirá con la conversación.'}</p></section><section><h3><span>02</span> Requerimientos identificados</h3>{data.requirements.length?data.requirements.map(r=><article className="requirement" key={r.id}><strong>{r.id} · {r.description}</strong><p className="small">{r.priority==='must'?'Imprescindible':'Deseable'} · {r.systemNote}</p><blockquote>“{r.source}”</blockquote></article>):<p className="muted">Todavía no hay requisitos confirmados.</p>}</section><section><h3><span>03</span> Preguntas pendientes</h3>{data.questions.length?data.questions.map(q=><article className="question" key={q.id}><strong>{q.question}</strong><p>{q.reason}</p>{q.blocksEstimate&&<span className="tag amber">Bloquea estimación</span>}</article>):<p className="muted">Sin preguntas pendientes.</p>}</section><section><h3><span>04</span> Riesgos y ambigüedades</h3>{data.risks.length?data.risks.map(r=><article className={`risk ${r.severity}`} key={r.id}><strong>{r.severity==='red'?'Alto':r.severity==='yellow'?'Atención':'Controlado'} · {r.description}</strong><p>{r.impact}</p><p className="small">{r.mitigation||'Falta definir una mitigación.'}</p></article>):<p className="muted">El análisis aún no identificó riesgos.</p>}</section><section><h3><span>05</span> Alcance</h3>{editableSections.slice(1,5).map(([key,label])=><div key={key}><h4>{label}</h4><List items={data[key] as string[]}/></div>)}</section><section><h3><span>06</span> Estimación orientativa</h3>{data.estimates.length?<div className="table-scroll"><table><thead><tr><th>Módulo</th><th>Horas</th><th>Incertidumbre</th></tr></thead><tbody>{data.estimates.map((e,i)=><tr key={i}><td>{e.module}</td><td>{e.minHours}–{e.maxHours}</td><td>{e.uncertainty}</td></tr>)}</tbody><tfoot><tr><th>Total</th><td>{data.estimates.reduce((n,e)=>n+e.minHours,0)}–{data.estimates.reduce((n,e)=>n+e.maxHours,0)}</td><td>horas</td></tr></tfoot></table></div>:<p className="muted">Se estimará cuando haya contexto suficiente.</p>}<p className="small muted">Rango orientativo, no precio final ni compromiso de entrega.</p></section><section><h3><span>07</span> Próximos pasos</h3><List items={data.nextSteps}/></section>{!locked&&<button className="button secondary full" onClick={edit}>Editar {professional?'documento':'secciones habilitadas'} ↗</button>}</div>:<div className="empty-state"><h3>Las buenas preguntas vienen primero.</h3><p>Contá qué necesitás en el chat. Acá irá tomando forma el Brief.</p></div>}</section>;
}
function List({items}:{items:string[]}){return items.length?<ul>{items.map((item,i)=><li key={i}>{item}</li>)}</ul>:<p className="muted">Por definir.</p>;}
